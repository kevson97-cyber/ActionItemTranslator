// Shared Claude conversation URL for the seeded prep curriculum.
// Paste your real claude.ai conversation URL here (or set
// NEXT_PUBLIC_CLAUDE_THREAD_URL in .env.local) once you've started the
// prep thread — every curriculum card's "Continue in Claude" link uses it.
export const CLAUDE_THREAD_URL = process.env.NEXT_PUBLIC_CLAUDE_THREAD_URL ?? '';
